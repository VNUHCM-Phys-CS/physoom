import BookingClient from "@/ui/BookingClient";
import Card from "@/ui/Card";

const Page = async () => {
  return (
    <div>
      <h4>Booking</h4>
      <BookingClient />
    </div>
  );
};
export default Page;
