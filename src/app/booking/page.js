import BookingClient from "@/ui/BookingClient";
import Card from "@/ui/Card";

const Page = async () => {
  return (
    <div>
      <div className="prose">
        <h4>Booking</h4>
      </div>
      <BookingClient />
    </div>
  );
};
export default Page;
